diff --git a/codex-rs/code-mode/Cargo.toml b/codex-rs/code-mode/Cargo.toml
--- a/codex-rs/code-mode/Cargo.toml
+++ b/codex-rs/code-mode/Cargo.toml
@@ -12,10 +12,12 @@ sandbox = ["v8/v8_enable_sandbox"]
 [dependencies]
 codex-code-mode-protocol = { workspace = true }
 codex-protocol = { workspace = true }
-deno_core_icudata = { workspace = true }
 futures = { workspace = true }
 serde_json = { workspace = true }
 tokio = { workspace = true, features = ["io-util", "macros", "process", "rt", "sync", "time"] }
 tokio-util = { workspace = true, features = ["rt"] }
 tracing = { workspace = true }
+
+[target.'cfg(not(all(target_arch = "x86", target_os = "linux", target_env = "musl")))'.dependencies]
+deno_core_icudata = { workspace = true }
 v8 = { workspace = true }
diff --git a/codex-rs/code-mode/src/lib.rs b/codex-rs/code-mode/src/lib.rs
--- a/codex-rs/code-mode/src/lib.rs
+++ b/codex-rs/code-mode/src/lib.rs
@@ -1,6 +1,17 @@
+#[cfg(not(all(target_arch = "x86", target_os = "linux", target_env = "musl")))]
 mod cell_actor;
 mod remote_session;
+#[cfg(not(all(target_arch = "x86", target_os = "linux", target_env = "musl")))]
 mod runtime;
+#[cfg_attr(
+    all(target_arch = "x86", target_os = "linux", target_env = "musl"),
+    path = "service_unavailable.rs"
+)]
 mod service;
+#[cfg(not(all(target_arch = "x86", target_os = "linux", target_env = "musl")))]
 mod session_runtime;
+#[cfg_attr(
+    all(target_arch = "x86", target_os = "linux", target_env = "musl"),
+    path = "v8_init_unavailable.rs"
+)]
 mod v8_init;

diff --git a/codex-rs/core/Cargo.toml b/codex-rs/core/Cargo.toml
index 23b5774f5..d17ec6172 100644
--- a/codex-rs/core/Cargo.toml
+++ b/codex-rs/core/Cargo.toml
@@ -126,5 +126,10 @@ which = { workspace = true }
 whoami = { workspace = true }
 
+# Use portable crypto implementations in the 32-bit musl guest used by iSH.
+[target.'cfg(all(target_arch = "x86", target_os = "linux", target_env = "musl"))'.dependencies]
+blake3 = { version = "1", features = ["pure"] }
+openssl-sys = { workspace = true, features = ["vendored"] }
+
 # Build OpenSSL from source for musl builds.
 [target.x86_64-unknown-linux-musl.dependencies]
 openssl-sys = { workspace = true, features = ["vendored"] }
diff --git a/codex-rs/linux-sandbox/src/landlock.rs b/codex-rs/linux-sandbox/src/landlock.rs
--- a/codex-rs/linux-sandbox/src/landlock.rs
+++ b/codex-rs/linux-sandbox/src/landlock.rs
@@ -166,6 +166,18 @@ fn install_landlock_rules_on_current_thread(
 ///
 /// The filter is applied to the current thread so only the sandboxed child
 /// inherits it.
+#[cfg(all(target_arch = "x86", target_os = "linux", target_env = "musl"))]
+fn install_network_seccomp_filter_on_current_thread(
+    _mode: NetworkSeccompMode,
+) -> std::result::Result<(), SandboxErr> {
+    // seccompiler has no 32-bit x86 backend, and iSH does not implement the
+    // host seccomp/namespaces used by Codex's Linux sandbox. CodexPad starts
+    // threads with danger-full-access inside the iPad-contained iSH guest, so
+    // this code is retained only to make the upstream crate target-portable.
+    Ok(())
+}
+
+#[cfg(not(all(target_arch = "x86", target_os = "linux", target_env = "musl")))]
 fn install_network_seccomp_filter_on_current_thread(
     mode: NetworkSeccompMode,
 ) -> std::result::Result<(), SandboxErr> {
diff --git a/codex-rs/Cargo.lock b/codex-rs/Cargo.lock
index a44af07b5..47b061a55 100644
--- a/codex-rs/Cargo.lock
+++ b/codex-rs/Cargo.lock
@@ -2619,6 +2619,7 @@ dependencies = [
  "assert_matches",
  "async-channel",
  "base64 0.22.1",
+ "blake3",
  "bm25",
  "chrono",
  "clap",
diff --git a/codex-rs/Cargo.toml b/codex-rs/Cargo.toml
index 501a410..e3874db 100644
--- a/codex-rs/Cargo.toml
+++ b/codex-rs/Cargo.toml
@@ -274,7 +274,7 @@ assert_matches = "1.5.0"
 async-channel = "2.3.1"
 async-io = "2.6.0"
 async-stream = "0.3.6"
-aws-config = "1"
+aws-config = { version = "1", default-features = false, features = ["credentials-process", "default-https-client", "rt-tokio"] }
 aws-credential-types = "1"
 aws-sigv4 = "1"
 aws-types = "1"
@@ -333,7 +333,7 @@ insta = "1.46.3"
 inventory = "0.3.19"
 itertools = "0.14.0"
 jsonptr = { version = "0.7.1", default-features = false }
-jsonwebtoken = "9.3.1"
+jsonwebtoken = { version = "=10.2.0", default-features = false, features = ["aws_lc_rs", "use_pem"] }
 keyring = { version = "3.6", default-features = false }
 landlock = "0.4.4"
 lazy_static = "1"
@@ -529,5 +529,10 @@ strip = "symbols"
 [profile.release]
 lto = "thin"
+
+# aws-lc-sys's CMake builder only supports OPENSSL_NO_ASM with OPT_LEVEL=0.
+# Keep the rest of Codex optimized while producing portable crypto for iSH.
+[profile.release.package.aws-lc-sys]
+opt-level = 0
 debug = "line-tables-only"
 split-debuginfo = "off"
 # Keep release binaries symbolicateable until packaging has archived the
diff --git a/codex-rs/Cargo.lock b/codex-rs/Cargo.lock
index 47b061a55..17cdbae 100644
--- a/codex-rs/Cargo.lock
+++ b/codex-rs/Cargo.lock
@@ -770,8 +770,6 @@ dependencies = [
  "aws-credential-types",
  "aws-runtime",
  "aws-sdk-signin",
- "aws-sdk-sso",
- "aws-sdk-ssooidc",
  "aws-sdk-sts",
  "aws-smithy-async",
  "aws-smithy-http",
@@ -787,7 +785,6 @@ dependencies = [
  "http 1.4.0",
  "p256",
  "rand 0.8.6",
- "ring",
  "sha2 0.10.9",
  "time",
  "tokio",
@@ -8508,14 +8505,16 @@ checksum = "a5a3cc660ba5d72bce0b3bb295bf20847ccbb40fd423f3f05b61273672e561fe"
 [[package]]
 name = "jsonwebtoken"
-version = "9.3.1"
+version = "10.2.0"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "5a87cc7a48537badeae96744432de36f4be2b4a34a05a5ef32e9dd8a1c169dde"
+checksum = "c76e1c7d7df3e34443b3621b459b066a7b79644f059fc8b2db7070c825fd417e"
 dependencies = [
+ "aws-lc-rs",
  "base64 0.22.1",
+ "getrandom 0.2.17",
  "js-sys",
  "pem",
- "ring",
  "serde",
  "serde_json",
+ "signature",
  "simple_asn1",
 ]
